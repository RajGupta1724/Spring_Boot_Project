databases:
  - name: emp-db
    plan: free
    databaseName: empdb
    user: empuser

services:
  - type: web
    name: emp-project-backend
    plan: free
    runtime: docker
    rootDir: emp-project
    envVars:
      - key: DB_HOST
        fromDatabase:
          name: emp-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: emp-db
          property: port
      - key: DB_NAME
        fromDatabase:
          name: emp-db
          property: database
      - key: DATABASE_USERNAME
        fromDatabase:
          name: emp-db
          property: user
      - key: DATABASE_PASSWORD
        fromDatabase:
          name: emp-db
          property: password
      - key: FRONTEND_URL
        value: https://emp-project-view.onrender.com

  - type: web
    name: emp-project-view
    plan: free
    runtime: node
    rootDir: emp-project/emp-project-view
    buildCommand: npm install && npm install -g serve && npm run build
    startCommand: serve -s build -l $PORT
    envVars:
      - key: REACT_APP_API_URL
        value: https://emp-project-backend.onrender.com
